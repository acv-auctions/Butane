const defaultTheme = require('tailwindcss/defaultTheme')

module.exports = {
  content: ["./src/**/*.{vue,ts}"],
  theme: {
    fontFamily: {
      'terminal': ["'Glass TTY'", "sans-serif"]
    }
  },
  plugins: [],
}
